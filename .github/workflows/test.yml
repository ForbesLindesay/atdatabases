# !!! This file is auto-generated, do not edit by hand !!!
# To make changes, edit .github/workflows-src/test.ts and then run:
#
#   github-actions-workflow-builder --directory ".github/workflows-src""

name: Test
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14.x
          registry-url: https://registry.npmjs.org
      - name: Get yarn cache directory path
        run: echo "::set-output name=dir::$(yarn cache dir)"
        id: step_3
      - name: Enable Cache
        uses: actions/cache@v2
        with:
          path: ${{ steps.step_3.outputs.dir }}
          key: ${{runner.os}}-14.x-${{hashFiles('yarn.lock')}}
      - run: yarn install --prefer-offline
      - run: yarn build:all
      - name: "Save output: build"
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: |-
            packages/*/lib
            packages/*/.last_build
    outputs:
      output: build
  test:
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14.x
          registry-url: https://registry.npmjs.org
      - name: Get yarn cache directory path
        run: echo "::set-output name=dir::$(yarn cache dir)"
        id: step_3
      - name: Enable Cache
        uses: actions/cache@v2
        with:
          path: ${{ steps.step_3.outputs.dir }}
          key: ${{runner.os}}-14.x-${{hashFiles('yarn.lock')}}
      - run: yarn install --prefer-offline
      - name: "Load output: ${{needs.build.outputs.output}}"
        uses: actions/download-artifact@v2
        with:
          name: ${{ needs.build.outputs.output }}
          path: packages/
      - run: yarn build
